{
  "name": "@stock/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 127.0.0.1 --port ${PORT:-3001} --reload",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-3001}",
    "test": "python3 -m pytest"
  }
}
